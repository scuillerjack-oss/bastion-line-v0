{
  "name": "bastion-line-v0",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "BASTION LINE — Tower Defense mobile compact en portrait (Web/PWA, futur Android). Projet 6, V0.",
  "scripts": {
    "dev": "vite",
    "dev:lan": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "test": "node --test",
    "test:mobile": "node scripts/check-mobile.mjs"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "vite": "^5.4.10"
  }
}
